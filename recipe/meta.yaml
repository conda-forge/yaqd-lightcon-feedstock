{% set name = "yaqd-lightcon" %}
{% set version = "2025.12.0" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name | replace('-', '_') }}-{{ version }}.tar.gz
  sha256: 1a424ef31766c543ad1ad5f37475b5ec27deb629cb2150432b08ecbec2d7ca7f

build:
  number: 0
  noarch: python
  entry_points:
    - yaqd-lightcon-topas4-motor = yaqd_lightcon._lightcon_topas4_motor:LightconTopas4Motor.main
    - yaqd-lightcon-topas4-shutter = yaqd_lightcon._lightcon_topas4_shutter:LightconTopas4Shutter.main
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - python >=3.7
    - pip
    - flit
    - aiohttp
    - yaqd-core >=2020.10.1
  run:
    - python >=3.7
    - aiohttp
    - yaqd-core >=2020.10.1

test:
  imports:
    - yaqd_lightcon
  commands:
    - pip check
    - yaqd-lightcon-topas4-motor --help
    - yaqd-lightcon-topas4-shutter --help
  requires:
    - pip

about:
  home: https://yaq.fyi
  summary: yaq daemons for hardware from Light Conversion
  dev_url: https://gitlab.com/yaq/yaqd-lightcon
  license: LGPL-3.0-only
  license_file: LICENSE

extra:
  recipe-maintainers:
    - untzag
    - ksunden
